diff --git a/src/utils.py b/src/utils.py
index 1af1b28..6e23cfc 100644
--- a/src/utils.py
+++ b/src/utils.py
@@ -112,9 +112,16 @@ def query_server(
     - Gemini / Google AI Studio
     - Fireworks (OpenAI compatbility)
     - SGLang (Local Server)
+    - Ollama (Local Server)
     """
     # Select model and client based on arguments
     match server_type:
+        case "ollama":
+            client = OpenAI(
+                api_key="ollama",   # required, but not actually used
+                base_url=f"http://{server_address}:{server_port}/v1",
+            )
+            model = model_name
         case "sglang":
             url = f"http://{server_address}:{server_port}"
             client = OpenAI(
@@ -395,6 +402,13 @@ SERVER_PRESETS = {
         "temperature": 0.1,
         "max_tokens": 8192,
     },
+    "ollama": {
+        "model_name": "codellama:7b",
+        "temperature": 0.0,
+        "server_address": "localhost",
+        "server_port": 11434,
+        "max_tokens": 4096,
+    }
 }
 
 
